# Reference documentation

## Location and purpose

This file applies to `docs/reference/`. It is reserved for stable, lookup-oriented documentation such as exported APIs, configuration options, compatibility requirements, and error behavior.

## Contents at this level

- `AGENTS.md` — this contextual note; there are currently no reference pages.

## Current state and next reflections

The reference section is a blank foundation. It should be populated after the package API is defined, starting with installation requirements, the public entry point, key registration/authentication concepts, supported runtimes, and a concise security model for handling WebAuthn/U2F credentials.
